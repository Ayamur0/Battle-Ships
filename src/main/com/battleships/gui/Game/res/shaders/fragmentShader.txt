#version 400 core

//converts per vertex data to per pixel data for screen to display

in vec2 pass_textureCoords //get textureCoords from vertexShader

out vec4 out_Color;

uniform sampler2d textureSampler;

void main(){

    out_Color = texture(textureSampler, pass_textureCoords); //return color of pixel at coordinates
}